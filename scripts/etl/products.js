var product = {
  id: id(this),
  name: this.name,
  type: this.productType,
};

loadToproducts(product);

var product = {
  id: id(this),
  name: this.name,
  type: 'pack',
};

loadToproducts(product);

var product = {
  id: id(this),
  name: this.name,
  type: 'medication',
};

loadToproducts(product);
